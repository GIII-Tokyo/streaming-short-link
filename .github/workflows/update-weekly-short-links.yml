name: Update weekly Short.io links

on:
  # Saturday 21:00 JST
  schedule:
    - cron: "0 12 * * 6"

  workflow_dispatch:
    inputs:
      profile:
        description: Broadcast profile
        required: true
        default: all
        type: choice
        options:
          - all
          - sunday-service
          - test-stream

      target_date:
        description: >
          Broadcast date in YYYY-MM-DD. Leave empty for next Sunday.
        required: false
        type: string

      dry_run:
        description: Validate without updating Short.io
        required: true
        default: true
        type: boolean

concurrency:
  group: weekly-shortio-update
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  update-links:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - name: Find broadcasts and update Short.io
        env:
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          GOOGLE_REFRESH_TOKEN: ${{ secrets.GOOGLE_REFRESH_TOKEN }}

          SHORT_IO_API_KEY: ${{ secrets.SHORT_IO_API_KEY }}
          SHORT_IO_LINK_ID: ${{ secrets.SHORT_IO_LINK_ID }}
          SHORT_IO_TEST_LINK_ID: ${{ secrets.SHORT_IO_TEST_LINK_ID }}

          YOUTUBE_TIME_ZONE: ${{ vars.YOUTUBE_TIME_ZONE }}
          YOUTUBE_PLAYLIST_ID: ${{ vars.YOUTUBE_PLAYLIST_ID }}
          YOUTUBE_START_TIME: ${{ vars.YOUTUBE_START_TIME }}
          YOUTUBE_TEST_START_TIME: ${{ vars.YOUTUBE_TEST_START_TIME }}

          YOUTUBE_BROADCAST_CONFIG: config/youtube-broadcasts.json
          PROCESS_MODE: shortio-only

          SELECTED_PROFILE: >-
            ${{ github.event_name == 'workflow_dispatch'
                && inputs.profile
                || 'all' }}

          TARGET_DATE: >-
            ${{ github.event_name == 'workflow_dispatch'
                && inputs.target_date
                || '' }}

          DRY_RUN: >-
            ${{ github.event_name == 'workflow_dispatch'
                && inputs.dry_run
                || false }}

          UPDATE_SHORT_IO: "true"

        run: node scripts/create-weekly-broadcast.mjs

